{
	"name": "HapticFrontend",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"postinstall": "bower install && tsd install && grunt update-libs && grunt build",
		"start": "grunt build && concurrent \"http-server website\" \"grunt watch\"",
		"build": "grunt build",
		"bower": "bower",
		"grunt": "grunt",
		"tsd": "tsd"
	},
	"devDependencies": {
		"bower": "1.4.x",
		"grunt-cli": "latest",
		"grunt": "~0.4.5",
		"grunt-contrib-concat": "0.5.x",
		"grunt-contrib-copy": "0.8.x",
		"grunt-contrib-less": "1.0.x",
		"grunt-contrib-uglify": "0.9.x",
		"grunt-contrib-watch": "0.6.x",
		"grunt-ts": "4.2.x",
		"grunt-tslint": "2.4.x",
		"tsd": "0.6.x",
		"http-server": "latest",
		"concurrently": "latest",
		"load-grunt-tasks": "latest"
	}
}
