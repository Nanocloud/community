<div class="dashboard section">

  <div class='card-wrapper'>
  {{#card-component title="Current registered users" content=users.length }}
    {{#link-to 'protected.users.new'}}
      <span class='color-primary'>Add users</span>
    {{/link-to}}
  {{/card-component}}

  {{#card-component title="Current online users" content=sessions.length }}
    {{#link-to 'protected.users'}}
      <span class='color-primary'>View users</span>
    {{/link-to}}
  {{/card-component}}
  {{#card-component title="Published applications" content=apps.length }}
    {{#link-to 'protected.apps'}}
      <span class='color-primary'>View apps</span>
    {{/link-to}}
  {{/card-component}}
  </div>

</div>
