<div class="section p-a-2">
  <h4>Applications</h4>
  <div class='content-wrapper'>
    <div class='drag-drop-wrapper'>
      {{drag-n-drop}}
    </div>
    <button class="btn btn-primary" {{ action "toggleSingleTab" "hapticDesktop" }}>Desktop</button>
    <table class="table application-table">
      <tbody>
        {{#each model as |application|}}
          {{#unless user.isNew}}
            <tr>
              <td class='icon-col'>
                {{#if application.iconContent}}
                  <img class='applications-icon' src="data:image/png;base64, {{ application.iconContent}}">
                {{else}}
                  <i class='material-icons'>video_label</i>
                {{/if}}
              </td>
              <td class='name'>{{application.alias}}</td>
              <td class='link' {{ action "toggleSingleTab" application.alias }}>
                <span class="icon">
                  <i class="material-icons">ondemand_video</i>
                </span>
                Start this app
              </td>
              <td class='link'>
                <span class="icon">
                  <i class="material-icons">clear</i>
                </span>
                Unpublish
              </td>
            </tr>
          {{/unless}}
        {{else}}
            No application available.
        {{/each}}
      </tbody>
    </table>
    <div class="clearfix"></div>
  </div>
</div>
